Some made up syntax for a made up documentation language.
This describes how nodes are encoded in create_node().

abstract struct Node(T1, T2) {
  field: {
    size: 2,
    name: group,
    purpose: Tags Syntax (10), Token (01), Trivium (00), and List/Missing (11).
  },
  union: {
    size: 8,
    name: T1
  },
  field: {
    size: 18,
    name: parent_idx,
    purpose: The NodeId of the parent of the current Node.
  },
  union: {
    size: 18,
    name: T2
  },
  field: {
    size: 18,
    name: idx,
    purpose: The unique NodeId of this Node
  }
}

final struct Syntax extends Node(Kind, FirstChildSiblingId) {}

final struct Token extends Node(Kind, TokenTextTriviumOffset) {}

final struct Trivium extends Node(Kind, SourceByteOffset) {}

final struct List extends Node(ChildCount, FirstChildSiblingId) {}

final struct Missing extends Node(Zero(2), Zero(8)) {}

embedded struct Kind {
  field: {
    size 8,
    name: kind,
    purpose: The InternedString<Kind> which uniquely identifies the kind of this
             Node when combined with the field "group".
  }
}

embedded struct FirstChildSiblingId {
  field: {
    size: 18,
    name: first_child_sibling_id,
    purpose: The SiblingId of the first child of this Node.
  }
}

embedded struct TokenTextTriviumOffset {
  field: {
    size: 18,
    name: token_text_trivium_offset,
    purpose: The NodeId of the Trivium that has the text of this Token.
  }
}

embedded struct SourceByteOffset {
  field: {
    size: 18,
    name: source_byte_offset,
    purpose: The index of the first byte in the source text.
  }
}

embedded struct ChildCount {
  field: {
    size: 8,
    name: child_count,
    purpose: The size of this list, or 255 to signify lists of length 255+.
  }
}

embedded struct Zero(T) {
  field: {
    size: T,
    name: _,
    purpose: All zeroed out bytes.,
    value: 0
  }
}

// Expanded views of all the final structs

final struct Syntax {
  field: {
    size: 2,
    name: group,
    value: 10,
    purpose: Tags Syntax (10), Token (01), Trivium (00), and List/Missing (11).
  },
  field: {
    size 8,
    name: kind,
    purpose: The InternedString<Kind> which uniquely identifies the kind of this
             Node when combined with the field "group".
  },
  field: {
    size: 18,
    name: parent_idx,
    purpose: The NodeId of the parent of the current Node
  },
  field: {
    size: 18,
    name: first_child_sibling_id,
    purpose: The SiblingId of the first child of this Node.
  },
  field: {
    size: 18,
    name: idx,
    purpose: The unique NodeId of this Node
  }
}

final struct Token {
  field: {
    size: 2,
    name: group,
    value: 01,
    purpose: Tags Syntax (10), Token (01), Trivium (00), and List/Missing (11).
  },
  field: {
    size 8,
    name: kind,
    purpose: The InternedString<Kind> which uniquely identifies the kind of this
             Node when combined with the field "group".
  },
  field: {
    size: 18,
    name: parent_idx,
    purpose: The NodeId of the parent of the current Node
  },
  field: {
    size: 18,
    name: token_text_trivium_offset,
    purpose: The NodeId of the Trivium that has the text of this Token.
  }
  field: {
    size: 18,
    name: idx,
    purpose: The unique NodeId of this Node
  }
}

final struct Trivium {
  field: {
    size: 2,
    name: group,
    value: 00,
    purpose: Tags Syntax (10), Token (01), Trivium (00), and List/Missing (11).
  },
  field: {
    size 8,
    name: kind,
    purpose: The InternedString<Kind> which uniquely identifies the kind of this
             Node when combined with the field "group".
  },
  field: {
    size: 18,
    name: parent_idx,
    purpose: The NodeId of the parent of the current Node
  },
  field: {
    size: 18,
    name: source_byte_offset,
    purpose: The index of the first byte in the source text.
  }
  field: {
    size: 18,
    name: idx,
    purpose: The unique NodeId of this Node
  }
}

final struct List {
  field: {
    size: 2,
    name: group,
    value: 11,
    purpose: Tags Syntax (10), Token (01), Trivium (00), and List/Missing (11).
  },
  field: {
    size: 8,
    name: child_count,
    purpose: The size of this list, or 255 to signify lists of length 255+.
  }
  field: {
    size: 18,
    name: parent_idx,
    purpose: The NodeId of the parent of the current Node
  },
  field: {
    size: 18,
    name: first_child_sibling_id,
    purpose: The SiblingId of the first child of this Node.
  },
  field: {
    size: 18,
    name: idx,
    purpose: The unique NodeId of this Node
  }
}

final struct Missing {
  field: {
    size: 2,
    name: group,
    value: 11,
    purpose: Tags Syntax (10), Token (01), Trivium (00), and List/Missing (11).
  },
  field: {
    size: 8,
    name: _,
    value: 0,
    purpose: All zeroed out bytes.
  }
  field: {
    size: 18,
    name: parent_idx,
    purpose: The NodeId of the parent of the current Node
  },
  field: {
    size: 8,
    name: _,
    value: 0,
    purpose: All zeroed out bytes.
  }
  field: {
    size: 18,
    name: idx,
    purpose: The unique NodeId of this Node
  }
}